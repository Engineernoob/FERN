# Learner wrapper
# RL learner (stub)
class Learner:
    def select_action(self, context: dict) -> str: return "retry_small_patch"
    def compute_reward(self, *args, **kwargs) -> float: return 1.0
    def record(self, *args, **kwargs): pass
