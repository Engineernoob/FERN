# Thompson sampling bandit
# Thompson sampling bandit (stub)
class ThompsonBandit:
    def select(self, context: dict, actions: list[str]) -> str:
        return actions[0]
    def update(self, context: dict, action: str, reward: float): pass
